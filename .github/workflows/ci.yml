name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    if: github.repository == 'Onemind-Services-LLC/actions'
    runs-on: ubuntu-22.04-sh
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: Onemind-Services-LLC/actions/actions/python-setup-install@3cc80b064ebe1023e05533efa15b47c43ead5ef9
        with:
          github-token: ${{ github.token }}

      - name: Run yamllint
        uses: Onemind-Services-LLC/actions/actions/yamllint@3cc80b064ebe1023e05533efa15b47c43ead5ef9
